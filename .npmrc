# Disable integrity checks for problematic packages
audit=false
fund=false
# Use npm registry with better integrity handling
registry=https://registry.npmjs.org/
# Disable strict SSL for problematic packages
strict-ssl=false
